import React from 'react';

import './navbar.css';

const Navbar = () => (
  <div className="navbar">
    <a href="#" className="navbar-link navbar-logo">Navbar</a>
    <a href="#" className="navbar-link">Home</a>
    <a href="#" className="navbar-link">About</a>
  </div>
);

export default Navbar;
